# KOReader Progress Sync (kosync)

ryOS embeds a KOReader-compatible Progress Sync server so e-ink devices (and
other kosync clients) can share reading progress with the Books app.

## Endpoints

Base URL (set this as the custom sync server in KOReader):

```text
https://<your-ryos-host>/api/kosync
```

| Method | Path | Purpose |
|--------|------|---------|
| GET | `/api/kosync/healthcheck` | `{ state: "OK" }` |
| POST | `/api/kosync/users/create` | Register kosync credentials |
| GET | `/api/kosync/users/auth` | Validate `X-Auth-User` + `X-Auth-Key` |
| PUT | `/api/kosync/syncs/progress` | Upsert progress |
| GET | `/api/kosync/syncs/progress/:document` | Fetch progress |

Protocol details match the [official koreader-sync-server](https://github.com/koreader/koreader-sync-server):
JSON bodies, `Accept: application/vnd.koreader.v1+json`, and MD5 password keys.

## Setup in KOReader

1. Open **Progress Sync** plugin settings.
2. Set **Custom sync server** to `https://<host>/api/kosync`.
3. **Register** with the **same username** as your ryOS account.
4. Choose a sync password (KOReader sends `md5(password)` as `X-Auth-Key`).
5. Prefer **filename** document matching when the EPUB basename matches the
   file in ryOS `/Books/` (e.g. `Meditations - Marcus Aurelius.epub`).
   Binary (partial MD5) matching also works once Books has hashed the file.

## How it bridges to Books

- Progress is stored in Redis under `integration:kosync:…` (kosync-native).
- On PUT, the server also writes Cloud Sync v2
  `bookshelf/progress:<path>` so logged-in Books clients receive the update
  over the normal sync pipeline (percentage-based restore when CFI is empty).
- On GET, kosync-native progress is merged with Books bookshelf progress
  (newer `timestamp` wins).
- Document IDs resolve to `/Books/….epub` via:
  1. cached docpath mapping
  2. synced `bookshelf/docmap:<path>` (`filenameMd5` / `partialMd5`)
  3. filename MD5 of files in the user's `files/item:/Books/…` sync KV

## Auth notes

Kosync credentials are separate from the ryOS login password (KOReader only
sends an MD5). Use the same **username** as ryOS so progress lands in that
user's Books library. Registration creates the kosync key only; it does not
create a ryOS account.
