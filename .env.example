# Local / deployment environment variables (DO NOT commit secrets)
# Copy this to .env.local and fill values locally or set env vars in your deployment platform.

# OpenAI — enables Ryo AI replies and transcription
OPENAI_API_KEY=your_openai_api_key_here

# Optional but required for full chat (message persistence & realtime)
# REDIS_KV_REST_API_URL=https://...
# REDIS_KV_REST_API_TOKEN=...
# PUSHER_APP_ID=...
# PUSHER_KEY=...
# PUSHER_SECRET=...
# PUSHER_CLUSTER=...

# Optional TTS
# ELEVENLABS_API_KEY=...

# Dev port (optional)
PORT=5173

# Dev-only toggle: run in mock mode when you don't have Pusher/Redis
# MOCK_CHAT=true
